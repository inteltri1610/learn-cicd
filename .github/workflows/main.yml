name: Hello Github Actions
on: [push]

jobs:
  say_hello:
    runs-on: ubuntu-test
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: Print Hello
        run: echo "Xin chào, code đã lên"
